import {Router} from 'express';const router=Router();router.get('/',async(req,res)=>{try{const city=(req.query.city||'').trim();if(!city)return res.status(400).json({message:'city is required'});const geo=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=en&format=json`),gd=await geo.json(),p=gd.results?.[0];if(!p)return res.status(404).json({message:'City not found'});const w=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${p.latitude}&longitude=${p.longitude}&current=temperature_2m,apparent_temperature,weather_code,wind_speed_10m&timezone=auto`),wd=await w.json();res.json({city:p.name,country:p.country,current:wd.current})}catch{res.status(500).json({message:'Weather unavailable'})}});export default router;
