import {Router} from 'express';import {askAssistant} from '../services/aiPlanner.js';const router=Router();router.post('/',async(req,res)=>{try{const{message,context}=req.body;if(!message)return res.status(400).json({message:'message is required'});res.json({answer:await askAssistant(message,context||{})})}catch(e){res.status(500).json({message:e.message})}});export default router;
