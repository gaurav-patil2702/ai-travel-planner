import 'dotenv/config';import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import {connectDB} from './config/db.js';import auth from './routes/auth.js';import trips from './routes/trips.js';import weather from './routes/weather.js';import assistant from './routes/assistant.js';
const app=express();app.use(helmet());app.use(cors({origin:process.env.CLIENT_URL || true}));app.use(express.json({limit:'1mb'}));app.use(morgan('tiny'));app.get('/api/health',(req,res)=>res.json({ok:true,service:'tripmind-api',time:new Date().toISOString()}));app.use('/api/auth',auth);app.use('/api/trips',trips);app.use('/api/weather',weather);app.use('/api/assistant',assistant);app.use((err,req,res,next)=>{console.error(err);res.status(500).json({message:'Unexpected server error'})});const port=process.env.PORT||5000;connectDB().catch(e=>console.error('MongoDB unavailable:',e.message));app.listen(port,()=>console.log(`TripMind API running on http://localhost:${port}`));
